name: Deploy to Raspberry Pi

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu  
  
    steps:
      - name: Clone
    
        uses: actions/checkout@v4

      - name: Run
      
        run: |
            set -ex
            docker build -t BucStop .
            docker stop BucStop || true
            docker rm BucStop || true
            docker run -d --name BucStop -p 80:80 BucStop
