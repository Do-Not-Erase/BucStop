name: Deploy to Raspberry Pi

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Raspberry Pi via SSH
        run: |
          ssh lwrog@10.147.19.64 << 'EOF'
            cd ~/BucStop/
            git pull origin main
            docker compose down
            docker compose build --pull
            docker compose up
          EOF
